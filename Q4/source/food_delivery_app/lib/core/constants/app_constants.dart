class AppConstants {
  AppConstants._();

  static const String appName = 'Food Delivery';
  static const Duration orderStatusInterval = Duration(seconds: 5);
  static const Duration simulatedDeliveryTime = Duration(minutes: 30);
}
